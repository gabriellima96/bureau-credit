spring:
  application:
    name: gateway
  cloud:
    config:
      hostname: localhost
      uri: http://${spring.cloud.config.hostname}:8888

eureka:
  instance:
    hostname: localhost

security:
  oauth2:
    resource:
      hostname: localhost