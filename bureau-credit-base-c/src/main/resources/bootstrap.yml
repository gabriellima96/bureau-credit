spring:
  application:
    name: basec
  cloud:
    config:
      hostname: localhost
      uri: http://${spring.cloud.config.hostname}:8888
  redis:
    host: localhost

eureka:
  instance:
    hostname: localhost

security:
  oauth2:
    resource:
      hostname: localhost