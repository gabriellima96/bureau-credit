spring:
  application:
    name: baseb
  cloud:
    config:
      hostname: localhost
      uri: http://${spring.cloud.config.hostname}:8888
  data:
    mongodb:
      host: localhost

eureka:
  instance:
    hostname: localhost

security:
  oauth2:
    resource:
      hostname: localhost
